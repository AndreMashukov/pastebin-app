{
  "name": "pastebin-reaper",
  "version": "0.1.0",
  "private": true,
  "description": "Pastebin reaper. Sweeps expired pastes on a schedule, deletes the S3 body and the DDB row (PastesTable), and lets the existing CDC trigger publish a PasteDeleted event so the public-bff lean view is cleaned up transitively. No API surface, no bus publish -- DDB stream is the sole event producer (invariant).",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.12.7",
    "esbuild": "^0.21.5",
    "serverless": "^4.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
